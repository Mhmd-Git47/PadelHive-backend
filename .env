# --- Server ---
NODE_ENV=production
PORT=3000

# --- Database (use BOTH styles so your code works either way) ---
# Individual PG vars (used by many pg clients)
PGUSER=padelhive_app
PGPASSWORD=<PASTE_YOUR_DB_PASS_HERE>
PGDATABASE=padelhive
PGHOST=127.0.0.1
PGPORT=5432

# Single DATABASE_URL (some ORMs prefer this)
DATABASE_URL=postgres://padelhive_app:<PASTE_YOUR_DB_PASS_HERE>@127.0.0.1:5432/padelhive

# --- App / CORS / Frontend ---
FRONTEND_URL=https://padelhivelb.com
CORS_ORIGINS=https://padelhivelb.com,https://www.padelhivelb.com

# --- JWT ---
JWT_SECRET=<RUN `openssl rand -hex 32` AND PASTE HERE>

# --- (Optional) your existing keys, if your code needs them ---
# CHALLONGE_API_KEY=...
# CHALLONGE_USERNAME=...
# ZOHO_EMAIL=support@padelhivelb.com
# ZOHO_APP_PASSWORD=<DO NOT COMMIT; rotate if it was ever pushed>